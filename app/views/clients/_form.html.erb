<%= simple_form_for(@client) do |f| %>
  <% if @client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% @client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="row">
    <div class="field input-field col s6">
      <i class="material-icons prefix">account_circle</i>
      <%= f.text_field :name, id: "icon_prefix" %>
      <%= label_tag "icon_prefix", "Name" %>
    </div>
    <div class="field input-field col s6">
      <%= f.text_field :email %>
      <%= label_tag "Email" %>
    </div>
  </div>
  <div class="row">
    <div class="field input-field col s6">
      <i class="material-icons prefix">business</i>
      <%= f.text_field :organization, id: "icon_business" %>
      <%= label_tag "icon_business", "Organization" %>
    </div>
    <div class="field input-field col s6">
      <%= f.text_field :address %>
      <%= label_tag "address" %>
    </div>
  </div>
  <div class="row">
    <div class="field input-field col s12">
      <i class="material-icons prefix">http</i>
      <%= f.text_field :url, id: "icon_http" %>
      <%= label_tag "icon_http", "URL" %>
    </div>
  </div>
  <div class="row">
    <div class="field col s12">
      <%= label_tag "Choose client language" %>
      <%= f.select :language, [['English', 'en'], ['Español', 'es']] %>
    </div>
  </div>

  <button class="btn waves-effect waves-light" type="submit" name="action">Submit
    <i class="material-icons">send</i>
  </button>
<% end %>
